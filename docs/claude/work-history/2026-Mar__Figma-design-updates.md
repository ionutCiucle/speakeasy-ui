# Figma Design Updates

**Branch:** `feature/add-design-docs`
**Date:** March 2026

## Summary

Design-only changes made directly to the Figma file (`lY99bcmVKephF9srgpDvbj`). No code changes.

---

## Changes

### Create Tab — Step 1 (`36:2`) — Currency dropdown header

- **Removed** flag circle (Ellipse node `36:26`) and "US" country label (node `36:27`) from the currency selector header.
- **Repositioned** currency code ("USD", node `36:28`) and name ("US Dollar", node `36:29`) to align from the left edge of the field.
- The dropdown now displays **currency code + full name only** (e.g. USD  US Dollar), keeping it simple and text-based.
